# -----------------------------------------------------------------------------
# Proxmox Deployment Templates
# Defines cluster topologies with direct VM specs
# -----------------------------------------------------------------------------

micro:
  instances:
    - name: cc-mixed-0
      placement_group: placement-group-1
      cores: 4
      memory: 7000
      workload_class: mixed-plane
      storage:
        - storage_pool: local-lvm
          size: 64
          interface: scsi0
        - storage_pool: local-lvm
          size: 64
          interface: scsi1
      tags:
        - ml
        - ml-mixed

tiny:
  instances:
    - name: cc-master-0
      placement_group: placement-group-1
      cores: 4
      memory: 7000
      workload_class: control-plane
      storage:
        - storage_pool: local-lvm
          size: 32
          interface: scsi0
      tags:
        - ml
        - ml-master

    - name: cc-worker-0
      placement_group: placement-group-1
      cores: 4
      memory: 7000
      workload_class: worker-general
      storage:
        - storage_pool: local-lvm
          size: 64
          interface: scsi0
        - storage_pool: local-lvm
          size: 64
          interface: scsi1
      tags:
        - ml
        - ml-worker

small:
  instances:
    # Control plane nodes
    - name: master-0
      placement_group: placement-group-1
      cores: 4
      memory: 7000
      workload_class: control-plane
      storage:
        - storage_pool: local-lvm
          size: 32
          interface: scsi0
      tags:
        - ml
        - ml-master

    - name: master-1
      placement_group: placement-group-2
      cores: 4
      memory: 7000
      workload_class: control-plane
      storage:
        - storage_pool: local-lvm
          size: 32
          interface: scsi0
      tags:
        - ml
        - ml-master

    - name: master-2
      placement_group: placement-group-3
      cores: 4
      memory: 7000
      workload_class: control-plane
      storage:
        - storage_pool: local-lvm
          size: 32
          interface: scsi0
      tags:
        - ml
        - ml-master

    # Worker nodes
    - name: worker-0
      placement_group: placement-group-1
      cores: 4
      memory: 7000
      workload_class: worker-general
      storage:
        - storage_pool: local-lvm
          size: 64
          interface: scsi0
        - storage_pool: local-lvm
          size: 64
          interface: scsi1
      tags:
        - ml
        - ml-worker

    - name: worker-1
      placement_group: placement-group-2
      cores: 4
      memory: 7000
      workload_class: worker-general
      storage:
        - storage_pool: local-lvm
          size: 64
          interface: scsi0
        - storage_pool: local-lvm
          size: 64
          interface: scsi1
      tags:
        - ml
        - ml-worker

    - name: worker-2
      placement_group: placement-group-3
      cores: 4
      memory: 7000
      workload_class: worker-general
      storage:
        - storage_pool: local-lvm
          size: 64
          interface: scsi0
        - storage_pool: local-lvm
          size: 64
          interface: scsi1
      tags:
        - ml
        - ml-worker

small3m3w:
  instances:
    # Control plane nodes - distributed across placement groups
    - name: m-0
      placement_group: placement-group-1
      cores: 4
      memory: 7000
      workload_class: control-plane
      storage:
        - storage_pool: local-lvm
          size: 64
          interface: scsi0
      tags:
        - ml
        - ml-master

    - name: m-1
      placement_group: placement-group-2
      cores: 4
      memory: 7000
      workload_class: control-plane
      storage:
        - storage_pool: local-lvm
          size: 64
          interface: scsi0
      tags:
        - ml
        - ml-master

    - name: m-2
      placement_group: placement-group-3
      cores: 4
      memory: 7000
      workload_class: control-plane
      storage:
        - storage_pool: local-lvm
          size: 64
          interface: scsi0
      tags:
        - ml
        - ml-master

    # Worker nodes - distributed across placement groups
    - name: w-0
      placement_group: placement-group-1
      cores: 4
      memory: 7000
      workload_class: worker-general
      storage:
        - storage_pool: local-lvm
          size: 64
          interface: scsi0
        - storage_pool: local-lvm
          size: 64
          interface: scsi1
      tags:
        - ml
        - ml-worker

    - name: w-1
      placement_group: placement-group-2
      cores: 4
      memory: 7000
      workload_class: worker-general
      storage:
        - storage_pool: local-lvm
          size: 64
          interface: scsi0
        - storage_pool: local-lvm
          size: 64
          interface: scsi1
      tags:
        - ml
        - ml-worker

    - name: w-2
      placement_group: placement-group-3
      cores: 4
      memory: 7000
      workload_class: worker-general
      storage:
        - storage_pool: local-lvm
          size: 64
          interface: scsi0
        - storage_pool: local-lvm
          size: 64
          interface: scsi1
      tags:
        - ml
        - ml-worker
