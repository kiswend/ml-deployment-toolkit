apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: auth
  namespace: ory
spec:
  parentRefs:
    - name: main-gateway
      namespace: platform-system
  hostnames:
    - "auth.${domain}"
  rules:
    - backendRefs:
        - name: ory-kratos-selfservice-ui-kratos-selfservice-ui-node
          port: 80
