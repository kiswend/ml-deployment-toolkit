apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mcm
  namespace: ory
spec:
  parentRefs:
    - name: main-gateway
      namespace: platform-system
  hostnames:
    - "mcm.${domain}"
  rules:
    - backendRefs:
        - name: ory-oathkeeper-proxy
          port: 4455
