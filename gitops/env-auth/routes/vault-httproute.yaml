apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: vault
  namespace: vault
spec:
  parentRefs:
    - name: main-gateway
      namespace: platform-system
  hostnames:
    - "vault.${domain}"
  rules:
    - backendRefs:
        - name: vault
          port: 8200
