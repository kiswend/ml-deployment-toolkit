# LoadBalancer Service that receives extapi.${domain} traffic.
# CiliumEnvoyConfig intercepts traffic to this Service and applies mTLS verification.
apiVersion: v1
kind: Service
metadata:
  name: cilium-gateway-gw-extapi
  namespace: mojaloop
  annotations:
    external-dns.alpha.kubernetes.io/hostname: "extapi.${domain}"
spec:
  type: LoadBalancer
  ports:
    - name: https
      port: 443
      targetPort: 443
      protocol: TCP
